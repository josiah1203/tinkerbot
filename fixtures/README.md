# Fixtures

Fixtures are intentionally small repositories and artifacts used by the tests. They cover safe/unsafe test edits, changed production paths, import/export and route relationships, dynamic imports, and normalized mutation outcomes.
