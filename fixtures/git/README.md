# Git stability fixtures

Temporary repositories exercise spaces, Unicode, CRLF, binary/status-only files, renames, invalid revisions, safe argv passing, timeouts, and idempotent worktree cleanup. Expected results are recorded in [`../stability/manifest.json`](../stability/manifest.json).
