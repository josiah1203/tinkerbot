# Action fixtures

Action validation is credential-free. Static fixtures cover `pull_request`, least-privilege permissions, local report fallback, malicious annotation text, summary truncation, and the absence of `pull_request_target`.
