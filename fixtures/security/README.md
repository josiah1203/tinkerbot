# Tinkerbot security fixtures

These fixtures contain metadata-only adversarial inputs for the local assurance boundary. They must not be executed, uploaded, or treated as repository source. Tests cover path normalization, forged webhook rejection, malformed/tampered evidence, stale and partial states, fork degradation, duplicate deliveries, and rerun races.
