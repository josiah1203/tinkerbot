# Impact fixture matrix

Each directory is a small static-analysis scenario used as a human-readable fixture. The real Git base/head integration fixture is exercised in `tests/integration.test.ts`; these files document the graph shapes that the parser must preserve.
