# Assurance conformance fixtures

The JSON fixtures are schema-valid and source-free. They cover the empty assurance bundle and the versioned evidence contract, including explicit `UNKNOWN` state, provenance, hashes, and the no-source-upload privacy invariant. Generated receipts and graph fixtures are covered by `tests/assurance.test.ts`.
